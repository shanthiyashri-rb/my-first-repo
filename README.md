# my-first-repo
This project analyzes weather data of different Indian cities by representing them as a graph, where each city is a node and weather similarity between cities is shown as edges. Using graph traversal algorithms like BFS and DFS, the system identifies connections and patterns across multiple cities.
